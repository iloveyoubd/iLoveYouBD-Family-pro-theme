# Custom System Instructions for iloveyoubd.com

All future code modifications, additions, and designs performed by the AI agent MUST adhere strictly to the rules defined in this file. This ensures consistency, high search engine rankings, safe monetization, and an cutting-edge user experience.

---

## 1. Google AdSense Friendship & Policy Compliance
* **Zero Policy Violations:** Never generate layouts that violate Google Publisher Policies. No adultcontent, copyright infringing streams, or spam.
* **No Deceptive UX:** Do not create UI elements that look like download buttons but are actually designed to fool users into clicking third-party ads. Every action must be transparent.
* **Clear Ad Containers:** All Google AdSense slots (including placeholders) must be clean, properly spaced, non-overlapping, and separated from interactive elements (buttons, inputs) by at least 20px to prevent accidental/invalid clicks.
* **No Auto-Redirects or Popups:** Avoid native code that initiates browser alerts, popup windows, or redirects to unknown affiliate destinations without the user's explicit interaction.

## 2. Search Engine Optimization (SEO) & Top 10 Google Indexing
* **Semantic HTML:** Structure page segments using correct semantic nesting (`<header>`, `<main>`, `<footer>`, `<section>`) and hierarchical heading tags (`<h1>`, `<h2>`, `<h3>`) with search-friendly keywords.
* **Fast Core Web Vitals:** Keep codebase lightweight. Do not import unnecessary heavy packages. Optimize images, use lazy loading for non-critical elements, and ensure rapid server response times.
* **Metadata & Social Graph:** Maintain proper document head title tags, descriptive metadata, Open Graph (og:title, og:description, og:image), and structured JSON-LD schemas where applicable.
* **Flawless Bengali Localization:** Output clear, grammatically pristine Bengali alongside matching English descriptions so Google Crawford Bots can understand both and index them efficiently for regional queries.

## 3. Ultra-Modern Future-Proof Design (2040 Style)
* **Visual Palette:** Utilize deep cosmic slates (`#070b13`, `#0d1527`), combined with high-contrast glowing accents like neon Cyan (`#00f0ff`), electric Violet, and rich Emerald greens.
* **Micro-Animations:** Use subtle entering transitions and interactive hover responses powered by `motion/react` to highlight interactive status.
* **Premium Grid Layouts:** Implement content structures inside clean grid cards (Bento Style) with generous negative space and balanced typographic hierarchy.
* **Futuristic Typography:** Rely on clean geometric display headings (e.g., Space Grotesk) paired with monospace subheadings (e.g., JetBrains Mono) for status dashboards.
* **Anti-AI Slop:** Ensure all dashboard indicators represent real, functioning indicators. Do not add mock logs, fake telemetry codes, or useless diagnostic numbers that clutter the screen without serving the user.
